import { ArrowRight, CheckCircle2, FileText, Code2, Rocket, ShieldCheck } from 'lucide-react';
import { Reveal, SectionHeading } from '@/components/ui';
import { workProcessSteps } from '@/data/company';

const icons = [FileText, Code2, Rocket, ShieldCheck];

export function WorkProcess() {
  // Return null here to temporarily hide the component without deleting the file or breaking imports
  return null;

  /* 
  // Original JSX hidden for now:
  return (
    <section id="how-we-work" className="relative bg-white py-16 lg:py-24 border-y border-navy-100 overflow-hidden">
      ...
    </section>
  );
  */
}